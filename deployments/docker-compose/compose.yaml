services:
  postgres-db:
    image: postgres:17
    container_name: postgres-db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data2
    environment:
      POSTGRES_USER: ${AMBULANCE_API_DB_USERNAME}
      POSTGRES_PASSWORD: ${AMBULANCE_API_DB_PASSWORD}
      POSTGRES_DB: ambulance

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: always
    ports:
      - "8082:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: pgadmin4@pgadmin.org
      PGADMIN_DEFAULT_PASSWORD: postrges
    depends_on:
      - postgres-db

volumes:
  db_data: {}